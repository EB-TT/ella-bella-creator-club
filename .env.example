# Copy to .env and fill in from the Supabase project settings (API section).
# Both values are safe for the browser bundle; the anon key is protected by RLS.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
